var vows = require('vows'),
    assert = require('assert');

// TODO: Fill in.
vows.describe('wrapped handler method').addBatch({
    'when there is a generic error from the the logic called by the wrapped handler method': 'NYI' /*{        
        topic: function () {  
        },

        'should have response status code of 500': function (resourceSpy) {
            assert.equal(true, false);
        },
    }*/,

    'when there is a custom error from the the logic called by the wrapped handler method': 'NYI'/*{        
        topic: function () {  
        },

        'should use associated response code': function (resourceSpy) {
            assert.equal(true, false);
        },
    }*/
}).export(module);