var assert = require('assert');

// TODO: Fill in.
describe('wrapped handler method', function() {
    describe('when you call a wrapped handler method': 'NYI'/*{
        
        topic: function () {  
        };

        it('should populate parameters based on values from request' : function(returned) {
            assert.isFalse(returned instanceof Error, returned.toString());
        }
    };*/
});