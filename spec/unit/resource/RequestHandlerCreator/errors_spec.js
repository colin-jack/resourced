describe('handling request', function() {
    describe('when there is a generic error from the the logic called by the handling request', function() {       
        it.skip('should have response status code of 500', function () {  
        });
    });

    describe('when there is a custom error from the the logic called by the handling request', function() {     
        it.skip('should use associated response code', function () {  
        });
    });
});