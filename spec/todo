// TODO: Tests
// No files in resources directory
// File that isn't a resource in the directory
// Object passed into Resource is invalid
//    URL is not a valid URL (or does express handle OK?) - use flatiron / revalidator 
//    No http overload specifying verb and method name doesn't tell us, individual resource
//    No http overload specifying verb and method name doesn't tell us, collection resource
//    More than one function in an object in respondsTo
// Resource URI is not valid/starting with "/", contains inappropriate characters
// RespondsTo had HTTP but method or URL are invalid, or url has no "/" at start.
// Cache
//   specifies -ve years, or years and some other metric.
//   specifies no where then it defaults to private
//   if ommitted then defaults to no caching
